class Subscription{
  DateTime startDate;
  DateTime endDate;
  String description;
  int quota ;
Subscription({
    required this.startDate,
    required this.endDate,
    required this.description,
    required this.quota
});
}